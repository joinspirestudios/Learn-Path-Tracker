import React from 'react';

import { PlaceholderScreen } from './PlaceholderScreen.js';

export function SettingsScreen() {
  return (
    <PlaceholderScreen
      title="Settings"
      intent="Preferences will appear here. No notifications, analytics, or trackers are wired in the foundation."
    />
  );
}

export default SettingsScreen;
