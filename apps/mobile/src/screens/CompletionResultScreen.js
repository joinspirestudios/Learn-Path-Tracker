import React from 'react';

import { PlaceholderScreen } from './PlaceholderScreen.js';

export function CompletionResultScreen() {
  return (
    <PlaceholderScreen
      title="Completion Result"
      intent="Day score and completion tier will be shown here. Tiers stay proof-backed, never gamified."
    />
  );
}

export default CompletionResultScreen;
