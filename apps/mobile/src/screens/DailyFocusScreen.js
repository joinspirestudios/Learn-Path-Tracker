import React from 'react';

import { PlaceholderScreen } from './PlaceholderScreen.js';

export function DailyFocusScreen() {
  return (
    <PlaceholderScreen
      title="Daily Focus"
      intent="A one-task-at-a-time proof session will run here. No scoring or proof capture is wired yet."
    />
  );
}

export default DailyFocusScreen;
